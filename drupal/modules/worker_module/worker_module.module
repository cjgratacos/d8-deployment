<?php


function worker_module_mail($key, &$message, $params){
    switch ($key) {
        case 'lead_mail':
            $body = $params[''];
            $message['body'] = $params['body'];
        default:
            
            break;
    }
}
