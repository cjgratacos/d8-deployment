worker_module.lead_form:
  path: '/lead_form'
  defaults:
    _form: '\Drupal\worker_module\Form\LeadForm'
    _title: 'The super lead form'
  requirements:
    _permission: 'access content'

worker_module.admin_lead_form:
  path: '/admin/process/lead_form'
  defaults:
    _form: '\Drupal\worker_module\Form\ManualProcessLeadForm'
    _title: 'The admin super lead form'
  requirements:
    _permission: 'administer site configutation'
